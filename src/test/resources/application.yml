spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: SA
    driver-class-name: org.h2.Driver
    initialization-mode: always